# Activate the Node major pinned in .node-version (helper: ~/.config/direnv/direnvrc)
use nvm

# Local node_modules/.bin on PATH so installed CLIs work without `npx`
PATH_add node_modules/.bin

dotenv_if_exists
